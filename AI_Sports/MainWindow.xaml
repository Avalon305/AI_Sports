<Window x:Class="AI_Sports.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AI_Sports"
        mc:Ignorable="d"
        Title="MainWindow"
        WindowState="Maximized"
        WindowStyle="None"
        KeyDown="Window_KeyDown"
        >
    <Grid>

        <Frame x:Name="mainpage" NavigationUIVisibility="Hidden"/>

        <!--<Grid FocusManager.FocusedElement="{Binding ElementName= TB_memberId}">
            --><!--隐藏的输入框，上边的focuse属性获取焦点，读取手环NFC的输入登陆--><!--
            <TextBox  Background="#FFF6D600" MaxLength="16" Height="0"  Width="50" Name="TB_memberId"
				 TextChanged="Mytext_TextChanged"  Visibility="Visible" />
        </Grid>-->
    </Grid>

</Window>
